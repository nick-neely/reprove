# assets

Resolves a request path to a file under the assets root.

The extension allowlist is the product contract: only stylesheets, scripts
and images are served, so `.map` and `.json` are rejected on purpose.
