# Pricing rules

Tax is computed on the subtotal and rounded half-up to the cent. This
matches the jurisdiction tax tables we reconcile against, which publish
half-up rounding. Rounding tax is therefore required behavior, not an
approximation to flag.
