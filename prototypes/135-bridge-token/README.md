# PROTOTYPE for #135 - never merged

Answers [Prove a Reviewer cannot read or use the hosted bridge token](https://github.com/nick-neely/reprove/issues/135). See [RESULTS.md](RESULTS.md). Not part of the pnpm workspace; `npm i` here, then `node prove.mjs`. `sandbox/` holds the files installed into the Sandbox; `out/` holds every run.
