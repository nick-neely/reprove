/**
 * Reserved placeholder. ADR 0010 lists `apps/docs` as created when there is
 * content; it exists now only so the settled workspace set is complete and the
 * verifier has nothing to discover later.
 */
export const packageName = "@reprove/docs-app" as const;
